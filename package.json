{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "generate": "node app.js",
    "json-server": "json-server --watch users.json --routes routes.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "json-server": "^0.17.1"
  }
}
